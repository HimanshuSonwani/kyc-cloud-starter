# API (api service only)
API_HOST=0.0.0.0
API_PORT=8000

# Redis (Upstash)
REDIS_URL=redis://default:REPLACE_PASSWORD@REPLACE_HOST:REPLACE_PORT

# S3-compatible storage (R2 or AWS S3)
S3_ENDPOINT= # For R2: https://<ACCOUNT_ID>.r2.cloudflarestorage.com ; leave empty for AWS
S3_REGION=auto  # For R2 use 'auto'. For AWS: ap-south-1, etc.
S3_ACCESS_KEY=REPLACE
S3_SECRET_KEY=REPLACE
S3_BUCKET=kyc-raw
